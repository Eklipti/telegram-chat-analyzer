# Команды и утилиты

Этот документ — справочник по всем командам CLI проекта.

Все запускается так:

```powershell
python main.py <команда> [аргументы]
```

По умолчанию:
- сырой экспорт ищется в `telegram/exports/raw_json/*.json` (берется самый новый файл);
- нормализованный файл ищется в `telegram/exports/processed_json/*.json` (берется самый новый; имена файлов — по хешу входного: `<hash>.json`);
- результаты пишутся в `output/YYYY_MM_DD_<hash>/`, где `<hash>` — первые 10 символов SHA256 входного JSON.

---

## 1. Полный конвейер (all)

Запускает весь основной процесс: `normalize -> agg -> social -> html -> mobile -> excel`.
Что именно считается и какие метрики формируются — см. [`docs/features.md`](features.md).

### Команда запуска
```powershell
python main.py all
```

### Аргументы
* `--input` (опциональный): путь к сырому JSON-экспорту. Если не указан — берется самый новый файл из `telegram/exports/raw_json/`.
* `--force` (опциональный): перезаписать существующий нормализованный файл на шаге normalize.

### Краткий пример результата
После выполнения появятся (пути по умолчанию):
* `telegram/exports/processed_json/<hash>.json` (имя = первые 10 символов SHA256 входного файла)
* `output/YYYY_MM_DD_<hash>/agg/` (агрегаты + `social_graph.json`)
* `output/YYYY_MM_DD_<hash>/report.html`
* `output/YYYY_MM_DD_<hash>/report.mobile.html`
* `output/YYYY_MM_DD_<hash>/report.xlsx`

---

## 2. Нормализация (normalize)

Нормализует сырой JSON и сохраняет результат в `telegram/exports/processed_json/<hash>.json`, где `<hash>` — первые 10 символов SHA256 входного файла (тот же хеш используется для каталога вывода отчётов).

В корень нормализованного JSON добавляется поле `original_file_name` — исходное имя файла (например, `2026_01_week1[+3].json`).

### Команда запуска
```powershell
python main.py normalize
```

### Аргументы
* `--input` (опциональный): путь к сырому JSON. Если не указан — берется самый новый файл из `telegram/exports/raw_json/`.
* `--force` (опциональный): перезаписать существующий нормализованный файл.

---

## 3. Агрегации (agg)

Считает базовую статистику и сохраняет JSON-агрегаты.

### Команда запуска
```powershell
python main.py agg
```

### Аргументы
* `--input` (опциональный): путь к нормализованному JSON. Если не указан — берется самый новый файл из `telegram/exports/processed_json/`.
* `--out-dir` (опциональный): каталог для сохранения агрегатов (по умолчанию `output/YYYY_MM_DD_<hash>/agg/`).

### Краткий пример результата
В каталоге вывода `output/YYYY_MM_DD_<hash>/agg/` появится набор JSON, включая (как минимум) `all_aggregates.json`.

---

## 4. Социальный граф (social)

Строит граф взаимодействий и дополнительные метрики (reply/mention, цитируемость, ссылки, CAPS и т.д.).

### Команда запуска
```powershell
python main.py social
```

### Аргументы
* `--input` (опциональный): путь к нормализованному JSON. Если не указан — берется самый новый файл из `telegram/exports/processed_json/`.
* `--out-dir` (опциональный): каталог для сохранения (по умолчанию `output/YYYY_MM_DD_<hash>/agg/`).

### Результат
* `output/YYYY_MM_DD_<hash>/agg/social_graph.json`

---

## 5. HTML-отчет (html)

Собирает единый HTML-отчет для десктопа из агрегатов.

### Команда запуска
```powershell
python main.py html
```

### Аргументы
* `--agg-dir` (опциональный): каталог агрегатов (по умолчанию `output/agg/`; при запуске команд с входным JSON используется хешированный каталог).
* `--out` (опциональный): путь к `report.html` (по умолчанию `output/report.html`).

---

## 6. Mobile HTML-отчет (mobile)

Собирает мобильную версию HTML-отчета.

### Команда запуска
```powershell
python main.py mobile
```

### Аргументы
* `--agg-dir` (опциональный): каталог агрегатов (по умолчанию `output/agg/`; при запуске команд с входным JSON — хешированный каталог).
* `--out` (опциональный): путь к `report.mobile.html` (по умолчанию `output/report.mobile.html`).

---

## 7. Excel-отчет (excel)

Генерирует Excel-отчет.

### Команда запуска
```powershell
python main.py excel
```

### Аргументы
* `--input` (опциональный): путь к нормализованному JSON. Если не указан — берется самый новый файл из `telegram/exports/processed_json/` (или будет создан из сырого).
* `--out` (опциональный): путь к `report.xlsx` (по умолчанию `output/YYYY_MM_DD_<hash>/report.xlsx`).

---

## 8. Стенограмма по авторам (author_and_text)

Группирует все сообщения по авторам и сохраняет их в хронологическом порядке.

### Команда запуска
```powershell
python main.py author_and_text
```
*(Скрипт автоматически найдет нормализованный файл или создаст его из сырого JSON)*

### Результат
В папке `output/YYYY_MM_DD_<hash>/author_text/` создаются два файла:
1.  `author_text_report.json` — структурированные данные для машинной обработки.
2.  `author_text_report.txt` — читаемый текстовый отчет (топ пользователей + тексты сообщений).

---

## 9. Профилирование JSON (params)

Инвентаризация структуры исходного JSON-файла: ключи, пути и типы данных. Полезно, чтобы понять, что именно пришло в экспорте.

### Команда запуска
```powershell
python main.py params
```
*(Скрипт автоматически найдет самый новый JSON-файл в папке `telegram/exports/raw_json/`)*

### Аргументы
* `--input` (опциональный): путь к сырому JSON.
* `--output` (опциональный): путь к итоговому `json_params.md` (по умолчанию `output/YYYY_MM_DD_<hash>/md/json_params.md`).

### Результат
* `output/YYYY_MM_DD_<hash>/md/json_params.md`

---

## 10. Контекстная история сообщений (context)

Утилита для создания текстовой истории сообщений за указанный период. Формирует хронологически упорядоченный список всех сообщений в простом текстовом формате. Поддерживает создание сжатой версии для оптимизации размера файла.

### Команда запуска
```powershell
python main.py context --date <дата_или_период>
```
*(Скрипт автоматически найдет нормализованный файл или создаст его из сырого JSON)*

### Аргументы
* `--date` (обязательный): дата или период для фильтрации сообщений:
    * `-1` — отчет за вчерашний день
    * `YYYY-MM-DD` — отчет за конкретный день (например, `2025-10-17`)
    * `YYYY-MM-DD_YYYY-MM-DD` — отчет за период (например, `2025-10-17_2025-10-20`)
* `--input` / `-i` (опциональный): путь к нормализованному JSON файлу.
* `--compress` (опциональный): создать дополнительно сжатую версию контекста.
* `--no-save` (опциональный): не сохранять **несжатую** версию (работает только с `--compress`).
* `--split` / `-s` (опциональный): разбить период на отдельные файлы для каждого дня (работает только с периодом).
* `--threads` / `-t` (опциональный): количество потоков для режима `--split` (по умолчанию 2, максимум 100).
* `--batch-size` (опциональный): размер батча сообщений для режима `--split` (по умолчанию 10000).
* `--min` (опциональный): минимальная длина сообщения для сжатой версии (по умолчанию 5).
* `--max` (опциональный): максимальная длина сообщения для сжатой версии (по умолчанию 250).

### Что делает `--compress`
Сжатие применяется к уже созданному файлу `context_<...>.txt` и формирует `context_<...>_compressed.txt`.

Это неархивирование, а “очистка/уплотнение” текста: сжатый файл меняет формат и содержимое, чтобы уменьшить шум и размер.

Правила:
- удаляются: 
  - пустые строки;
  - короткие сообщения (короче `--min` символов);
  - сообщения, состоящие только из упоминаний (`@username @user2 ...`);
  - сообщения, состоящие только из ссылок (`http://*`);
    - ссылки вида `http(s)://...` заменяются на `http://*`;
  - все эмодзи;
- из текста удаляются все “спецсимволы”, кроме букв/цифр/пробелов и `@ / * . , ! ? : ; -`;
- ограничиваются повторяющиеся символы (например, `??????` -> `??`);
- ограничиваются повторы одного слова подряд (например, `ах ах ах ах ах ах` -> максимум 5 раз);
- последовательные дубликаты сообщений у одного автора не сохраняются;
- длинные сообщения обрезаются до `--max` символов;
- меняется формат вывода: временные метки убираются, а ник упрощается:
  - берется только первое слово ника;
  - из ника убираются круглые скобки и эмодзи;
  - из ника убираются `.,!?:;-` (если после очистки ник не становится пустым).

Если указан `--no-save`, то после создания сжатой версии **несжатый** `context_<...>.txt` удаляется.

### Примеры использования
```powershell
# Обычная версия за конкретный день
python main.py context --date 2025-12-26

# Обычная + сжатая версия
python main.py context --date 2025-12-26 --compress

# Период с разбивкой на отдельные файлы для каждого дня
python main.py context --date 2025-12-26_2025-12-29 -s

# Только сжатая версия (без сохранения несжатой)
python main.py context --date 2025-12-26 --compress --no-save

# Настроить фильтры сжатия (длина сообщений)
python main.py context --date 2025-12-26 --compress --min 10 --max 200
```

### Краткий пример вывода (context.txt)
```text
Иван Иванов [2025-12-26 10:05:12]
"Привет! Кто-нибудь знает, как работает этот анализатор?"

Петр Петров [2025-12-26 10:07:45]
"Привет, Иван. Да, он парсит JSON экспорт из десктопной версии телеграма."
```

### Результат
В папке `output/YYYY_MM_DD_<hash>/context/` создается:
* `context_<дата>.txt` — полная текстовая история.
* `context_<дата>_compressed.txt` — оптимизированная версия.
